1. Want to be able to add CLA
	* [THEME]
	* -v verbose        Print out progress
	* -b backup         Backup any overwritten files in str/tmp directory
	* -c config         Change a single config for a single program (i.e change prompt)
	* --version
	* --help

2. Want to add a config file for default values of above options.
	* Default verbosity [1/0]
	* Default backup [1/0]
	* If default backup 1, max num backups [int]

3. Figure out how to do autocompletion

3. MAYBE add some kind of gui? idk
